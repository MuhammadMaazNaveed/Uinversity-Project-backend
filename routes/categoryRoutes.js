const express = require("express");
const router = express.Router();
const Category = require("../controllers/category.js");
// for Employee and employee crud operation
// router.post("/createCategory", Category.CreateCategory);
// router.get("/allCategorys", Category.getCategorys);
// router.get("/:id", Category.getCategory);
// router.post("/:id", Category.editCategory);
// router.delete("/:id", Category.deleteCategory);

module.exports = router;
